import React from "react";

const {
  Provider: ComponentServiceProvider,
  Consumer: ComponentServiceConsumer
} = React.createContext();

export { ComponentServiceProvider, ComponentServiceConsumer };
